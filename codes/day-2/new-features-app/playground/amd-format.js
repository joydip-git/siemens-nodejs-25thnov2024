//amd
// define('', [], function () {
//     const add = (a, b) => a + b
//     exports.addFn = add
// })

//umd
// require('', [], function () {
//     const add = (a, b) => a + b
//     exports.addFn = add
// })

(function () {
    const add = (a, b) => a + b
})()

    (function ([]) {
        const subtract = (a, b) => a - b
    })()