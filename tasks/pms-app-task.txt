1. create a repository (an array) with the given sample data
2. create a class (As follows) with basic CRUD operations in it
ProductManager
-----------------------
getAll() => returns all products from the array
get(id) => returns a product matching to that id
add(p) => adds a new product in the array
update(id,p) => updates an existing product in the array
delete(id) => delets an existing product in the array

3. invoke the methods 
4. create a class to represent your model sample data